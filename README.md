`npm install`

From APIs end point :
`node app.js`

for bulk
`node scripts/whitelist_bulk.js`

infura
`get key first`

geth:
`geth --rinkeby --unlock address --password password --rpc --rpcport 8549`

APIs
` post /whitelist`